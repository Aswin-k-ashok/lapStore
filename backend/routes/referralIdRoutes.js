// import express from 'express'
// const router = express.Router()
// import { protect, admin, active } from '../middleware/authMiddleware.js'

// import {
//   addReferralId,
//   getReferralId,
// } from '../controllers/referralIdController.js'

// router.route('/').get(protect, getReferralId).post(protect, addReferralId)

// // router.route('/userCount').get(getUserCount)
// export default router
